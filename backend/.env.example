# App config
APP_PORT=3000
NODE_ENV= # e.g. development | production

# Database config
DB_HOST=
DB_PORT=
DB_NAME=
DB_USERNAME=
DB_PASSWORD=
DB_DIALECT= # e.g. mysql | postgres | sqlite

# JWT config
JWT_SECRET=
JWT_EXPIRES_IN= # e.g. 1h, 7d

REFRESH_TOKEN_SECRET=
REFRESH_TOKEN_EXPIRES_IN= # e.g. 1h, 7d

# CORS config
FRONTEND_URL= # e.g. http://localhost:5173

# WhatsApp API configuration
WHATSAPP_API_URL=your_url
WHATSAPP_API_ACCCESS_TOKEN=your_token

# Configuración del Servidor SMTP usando Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=tu_correo_personal@gmail.com
SMTP_PASS=tu_contraseña_de_aplicacion_de_16_caracteres
MAIL_FROM="Startup CRM <no-reply@tu-dominio.com>" 